/**
 * This is a dummy sort that does nothing.
 */
public class None extends Sort {
    /**
     * {@inheritDoc}
     */
    public None(SimpleGrid grid, SubGrid subGrid, int column, int delay) {
        super(grid, subGrid, column, delay);
    }
}
